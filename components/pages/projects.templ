package pages

import (
	"mywebsite/components/shared"
	"mywebsite/components/partials"
	"mywebsite/ds"
)

templ ProjectsPage(pages_metadata *ds.Pages) {
	@shared.Page("Projects") {
		@partials.Nav(*pages_metadata)
		// TODO: add a list of projects. Also, maybe make projects runnable with wasm runtimes
	}
}


